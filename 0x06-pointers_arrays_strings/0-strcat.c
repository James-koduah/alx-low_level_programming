#include <stdio.h>
#include <string.h>
/**
 * _strcat - concate two strings together
 * @dest: kdkkddk
 * @src: kkdkdkd
 * Return: dkdkdkkd
 */
char *_strcat(char *dest, char *src)
{
	char *ptr;

	*ptr = strcat(dest, src);
	return (ptr);
}
