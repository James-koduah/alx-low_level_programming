#include <stdio.h>
#include <string.h>
/**
 * _strpbrk - kskksks
 * @s: ksksksksk
 * @accept: kskskks
 * Return: kskksk
 */
char *_strpbrk(char *s, char *accept)
{
	char *mat;

	mat = strpbrk(s, accept);
	return (mat);
}
