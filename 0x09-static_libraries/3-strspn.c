#include <stdio.h>
#include <string.h>
/**
 * _strspn - kskskks
 * @s: kdkdkdk
 * @accept: kskskksks
 * Return: kskksks
 */
unsigned int _strspn(char *s, char *accept)
{
	unsigned int ch;

	ch = strspn(s, accept);
	return (ch);
}
