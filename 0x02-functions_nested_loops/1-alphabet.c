#include <stdio.h>

/**
 * main - dddddd
 * Return: ksslsl
 */
int print_alphabet();
int main(void)
{
	print_alphabet();
	return (0);
}

int print_alphabet()
{
	int ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
