#include <stdio.h>
/**
 * main - jsjsj
 * Return: lslsl
 */
int main(void)
{
	int ch;
	int bb;

	for (ch = '0' ; ch <= '9' ; ch++)
	{
		for (bb = '0' ; bb <= '9' ; bb++)
		{
			putchar(ch);
			putchar(bb);
			putchar(',');
		}	
	
	}
	putchar('\n');
	return (0);
}
