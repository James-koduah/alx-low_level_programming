#include <stdio.h>
/**
 * main - lsllsl
 * Return: ksksksk
 */
int main(void)
{
	int ch;
	int bb;

	for (ch = '0' ; ch <= '9' ; ch++)
	{
		putchar(ch);
	}
	for (bb = 'a' ; bb <= 'f' ; bb++)
	{
		putchar(bb);
	}
	putchar('\n');
	return (0);
}
