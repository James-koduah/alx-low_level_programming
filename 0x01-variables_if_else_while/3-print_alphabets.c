#include <stdio.h>
/**
 * main - lslsll
 * Return: lslsls
 */
int main(void)
{
	int ch;
	int bb;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	for (bb = 'A' ; bb <= 'Z' ; bb++)
	{
		putchar(bb);
	}
	putchar('\n');
	return (0);
}
