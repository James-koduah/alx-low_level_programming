#include <stdio.h>
/**
 * print_most_numbers - dkkdkd
 * Return: kskskksk
 */
void main(void)
{
	int ch;

	for (ch = '0'; ch <= '9'; ch++)
	{
		if (ch == '2' || ch == '4')
		{	
		}
		else
		{
			putchar(ch);
		}
	}
	putchar('\n');
}
