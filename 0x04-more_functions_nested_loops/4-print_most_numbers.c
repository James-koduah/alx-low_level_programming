#include <stdio.h>
/**
 * print_numbers - dkkdkd
 * Return: kskskksk
 */
void print_numbers(void)
{
	int ch;

	for (ch = '0'; ch <= '9'; ch++)
	{
		if (ch != '2' || ch != '4')
		{
		putchar(ch);
		}
	}
	putchar('\n');
}
