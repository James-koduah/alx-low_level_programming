#include <stdio.h>
#include <string.h>
/**
 * _memcpy - kskskk
 * @dest: ksksksk
 * @src: ksksksk
 * @n: kskksks
 * Return: skskskks
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	memcpy(dest, src, sizeof(char) * n);

	return (dest);
}
